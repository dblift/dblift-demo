name: Scenario 03 - Rollback & Recovery

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scenario-runner.yml
    with:
      scenario-id: "03"
      scenario-name: "Rollback & Recovery"

