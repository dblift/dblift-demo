name: Scenario 08 - Multi-Module Project

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scenario-runner.yml
    with:
      scenario-id: "08"
      scenario-name: "Multi-Module Project"

