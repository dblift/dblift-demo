name: Scenario 06 - CI/CD Integration

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scenario-runner.yml
    with:
      scenario-id: "06"
      scenario-name: "CI/CD Integration"

