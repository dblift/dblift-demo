name: Scenario 07 - Tag-Based Deployment

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scenario-runner.yml
    with:
      scenario-id: "07"
      scenario-name: "Tag-Based Deployment"

