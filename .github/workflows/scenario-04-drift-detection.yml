name: Scenario 04 - Drift Detection

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scenario-runner.yml
    with:
      scenario-id: "04"
      scenario-name: "Drift Detection"

