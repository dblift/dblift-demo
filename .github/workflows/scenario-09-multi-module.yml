name: Scenario 09 - Multi-Module Project

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scenario-runner.yml
    with:
      scenario-id: "09"
      scenario-name: "Multi-Module Project"

